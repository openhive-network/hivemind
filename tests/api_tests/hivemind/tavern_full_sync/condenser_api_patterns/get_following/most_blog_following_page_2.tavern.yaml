---
  test_name: Hivemind condenser_api.get_following most blog following page 2
  #SELECT  COUNT(id) as record_count, follower FROM hive_follows WHERE state=1 GROUP BY(follower) ORDER BY record_count DESC LIMIT 3;;
  marks:
    - fullsynctest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_following most blog following page 2
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_following"
          params: ["marcusmalone","arynews236","blog",1000]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "most_blog_following_page_2"
            directory: "condenser_api_patterns/get_following"