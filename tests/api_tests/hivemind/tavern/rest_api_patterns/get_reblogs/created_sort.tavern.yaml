---
  test_name: Hivemind

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/rpc/get_reblogs"
        method: POST
        headers:
          content-type: application/json
          accept: application/json
        json:
          sort: "created"
          tag: ""
          observer: "funny"
          limit: 5
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_rest_response_with_pattern
