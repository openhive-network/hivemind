---
  test_name: Hivemind

  marks:
    - patterntest # original produced overlapping pages
    - postgrest_ready
    
  includes:
    - !include ../../common.yaml

  stages:
    - name: test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "tags_api.get_discussions_by_comments"
          params: {"start_author":"roadscape","start_permlink":"re-pfunk-can-we-have-advanced-mode-stick-20160510t161229520z","limit":1}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            ignore_tags: "<tags posts>"
