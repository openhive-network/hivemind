Gives information on given account role within given community.

method: "bridge.get_community_context"
params:
{
  "name": "{community}",
  
     mandatory, points to valid community (account in form of hive-number)

  "account": "{account}"

     mandatory, points to valid account (not necessarily subscibed to that community)
}
