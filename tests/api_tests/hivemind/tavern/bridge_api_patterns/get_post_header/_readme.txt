Gives very basic information on given post. Can be used to detect if post exists.

method: "bridge.get_post_header"
params:
{
  "author":"{author}", "permlink":"{permlink}"
  
     author + permlink : mandatory, point to valid post
}
