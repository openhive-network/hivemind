type: object
additionalProperties: false
required:
  - jsonrpc
  - result
  - id
properties:
  jsonrpc:
    type: string
    example: "2.0"
  id:
    type: integer
    example: 1
  result:
    type: array
    items:
      type: object
      required:
        - name
        - type
        - score
        - date
        - msg
        - url
      properties:
        name:
          type: string
          description: 'list name'
        blacklist_description:
          type: string
          description: 'description of blacklist'
        muted_list_description:
          type: string
          description: 'description of muted list'
