type: object
required:
  - jsonrpc
  - method
  - params
  - id
properties:
  jsonrpc:
    type: string
    default: "2.0"
  method:
    type: string
    enum:
      - tags_api.get_discussion
  params:
    type: object
    required:
      - author
      - permlink
    properties:
      author:
        type: string
        description: "Mandatory, points to valid post"
        example: "demotruk"
      permlink:
        type: string
        description: "Mandatory, points to valid post"
        example: "re-fyrstikken-re-vi1son-re-heiditravels-my-first-bad-experience-with-steemit-20160905t203611118z"
      observer:
        type: string
        description: "optional, used for muted votes and blacklists (that functionality was removed and now observer is completely ignored - it will most likely stay that way)"
        example: "ignoreall"
  id:
    type: integer
    default: 1
