type: object
required:
  - jsonrpc
  - method
  - params
  - id
properties:
  jsonrpc:
    type: string
    default: "2.0"
  method:
    type: string
    enum:
      - follow_api.get_follow_count
  params:
    type: object
    required:
      - account
    properties:
      account: 
        description: "mandatory, points to valid account"
        type: string
        example: "gtg"
  id:
    type: integer
    default: 1
